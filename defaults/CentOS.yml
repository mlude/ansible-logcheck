---

logcheck_user: "logcheck"
logcheck_group_for_logfile_access: "adm"

logcheck_group_for_config: "root"

logcheck_distribution_directory: "{{ ansible_distribution }}-{{ ansible_distribution_major_version }}"

logcheck_logfiles:
  - "/var/log/auth.log"
  - "/var/log/maillog"
  - "/var/log/messages"
  - "/var/log/secure"

